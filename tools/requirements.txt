# Dependencies for tools/md_to_pdf.py (see README "Printable PDFs").
# Chrome/Chromium is also required, on PATH, and is not installable via pip.
markdown-it-py>=3.0
mdit-py-plugins>=0.4
Pygments>=2.17
websocket-client>=1.7
pypdf>=4.0
